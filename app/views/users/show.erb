<h1>Welcome to <%=@user.username.capitalize%>'s BookShelf</h1>

<h3>Books <%=@user.username.capitalize%> is interested in:</h3>
<%@books.each do |book|%>
    <%if @user_books.find_by(:user_id => @user.id, :book_id => book.id) != nil
        if @user_books.find_by(:user_id => @user.id, :book_id => book.id).read == false %>
            <li><a href="/books/<%=book.slug%>"><%=book.title.split.map(&:capitalize).join(' ')%></a></li>
        <%end%>
    <%end%>
<%end%>


<h3>Books <%=@user.username.capitalize%> has read:</h3>
<%@books.each do |book|%>
    <%if @user_books.find_by(:user_id => @user.id, :book_id => book.id) != nil
        if @user_books.find_by(:user_id => @user.id, :book_id => book.id).read == true %>
            <li><a href="/books/<%=book.slug%>"><%=book.title.split.map(&:capitalize).join(' ')%></a></li>
        <%end%>
    <%end%>
<%end%>
<br>
<form action="/home" method="GET">
    <input type="submit" name="submit" value="Return Home">
</form><br>